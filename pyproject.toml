[project]
name = "sdv-on-pytorch"
version = "0.1.0"
description = "Minimal SDV + PyTorch 2.7 + CUDA 12.8 compatibility test for RTX 5070 Ti"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "sdv>=1.0.0",
    "torch==2.7.0",
    "torchvision==0.22.0",
    "torchaudio==2.7.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu128" }
torchvision = { index = "pytorch-cu128" }
torchaudio = { index = "pytorch-cu128" }
